[TOC]

# rule editor规则编辑器
控制关键行为:
+ 显示或隐藏对象
+ 启用或禁用对象
+ 为对象设置值
+ 验证对象的值
+ 执行功能以计算对象的值
+ 调用表单数据模型服务并执行操作
+ 设置对象的属性

## 编写规则
规则包括行为和条件,同时遵循条件-行为/行为-条件的结构

### 行为
例如何时成立(When), 显示, 隐藏, 启用, 禁用, 设置值和验证
### When
When规则类型遵循条件-操作-备用操作或者仅遵循条件-操作